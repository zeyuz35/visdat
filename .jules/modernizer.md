## 2025-02-14 - Replace purrr with base R vectorization in visdat internals
**Learning:** Checking types dynamically using `purrr::map_lgl` and length comparisons `~ length(.x) == 0` is less performant and creates unnecessary dependency bloat when iterating over standard vectors and data.frame structures. `vapply(data, is.list, logical(1))` and `lengths(x) == 0L` are fast and idiomatic base R replacements that preserve full compatibility and readability. We must keep `ifelse` structure with `is.na(x)` vs `lengths(x) == 0L` inside list vs non-list checks to maintain correct return factors and name dimensions without accidentally dropping list-column types. Tests should be run carefully, restoring deleted test snapshots if vdiffr throws false positives.
**Action:** In R package internal type checking loops, proactively replace `purrr` variants with `vapply`, `lengths`, and other native `base` vector functions for optimization.
