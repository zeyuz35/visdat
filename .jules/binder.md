## 2024-05-18 - Replacing purrr::set_names with stats::setNames\n**Learning:** When removing a dependency or finding a more base-R equivalent like stats::setNames over purrr::set_names, it is necessary to check if the package depends on the previous package in its DESCRIPTION file and to remove it from there too if not used elsewhere.\n**Action:** Verify if `purrr` is used anywhere else. Since `purrr` is heavily used in `visdat` (over 10 occurrences in the R directory), the overall package still depends on it, but making localized files less dependent on it is still a valid hygiene/structural improvement as Binder, even if the dependency cannot be removed entirely from the DESCRIPTION yet.
